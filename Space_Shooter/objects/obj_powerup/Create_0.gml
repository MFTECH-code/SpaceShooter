// Ele tem uma velocidade aleatória e irá em direções aleátorias

speed = irandom_range(-2, -5);
direction = irandom_range(180, 0);

v = random_range(.01, .1);
image_xscale = .5;
aumentando = true;
diminuindo = false;
cor = c_red;